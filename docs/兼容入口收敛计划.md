# 兼容入口收敛计划（已完成）

## 1. 结果

兼容入口下线已完成，执行日期：`2026-03-11`。

当前仅保留稳定主入口：

- CLI：`scripts/regression_cli.py`
- MCP：
  - `run_regression_by_scenario`
  - `run_regression_by_trace_pair`
  - `replay_and_diff_by_scenario`
  - `replay_and_diff_by_trace_ids`


## 2. 已下线入口

- CLI 兼容入口：`scripts/run_regression_only.py`
- MCP 兼容工具：
  - `run_api_regression`
  - `run_api_regression_advanced`


## 3. 迁移映射（历史记录）

- 旧 CLI：`python3 scripts/run_regression_only.py --old-scenario-id OLD --new-scenario-id NEW --api-path /a --api-path /b`
- 新 CLI：`python3 scripts/regression_cli.py "/a,/b" "OLD" "NEW"`

- 旧 MCP：`run_api_regression(...)` / `run_api_regression_advanced(...)`
- 新 MCP：`run_regression_by_scenario(...)`


## 4. 说明

- 本文档仅保留历史迁移记录。
- 后续不再维护兼容入口相关策略。
