# 快速开始

运行命令已统一维护在仓库根目录 [README.md](../README.md)。

请优先查看：

- `README` 的“环境准备”
- `README` 的“初始化命令”
- `README` 的“核心运行命令（稳定入口）”
- `README` 的“测试命令”
- `README` 的“报告与结果”

说明：

- 本文档不再重复维护命令示例，避免与代码实现产生偏差。
- 后续若命令参数发生变化，以 `README` 与脚本 `--help` 输出为准。
